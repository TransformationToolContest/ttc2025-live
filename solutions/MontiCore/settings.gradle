
pluginManagement {
  repositories {
      mavenCentral()
      if(("true").equals(getProperty('useLocalRepo'))){
        mavenLocal()
      }
      maven {
        url nexusUrl
      }

      gradlePluginPortal()    }
}
rootProject.name = 'MontiTrans'
